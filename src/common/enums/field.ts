export const SEARCH_ISSUE = ['image', 'detected.name'];
export const STORAGE_FIELD = 'storage';
export enum DIR {
    ASC = 'asc',
    DESC = 'desc'
}
export enum SORT_FIELD {
    image = 'image',
    storage = 'storage',
    createdAt = 'createdAt',
    DETECTED_NAME = 'detected.name'
}
